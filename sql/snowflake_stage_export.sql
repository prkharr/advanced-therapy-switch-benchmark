-- OPTIONAL SQL-ONLY WAREHOUSE UNLOAD EXAMPLE; NOT EXECUTED HERE.
-- Replace stage, unique run folder and seven canonical view placeholders.
-- Stage must already be approved and available to your role.
-- These queries write staged files, not files on your local computer.
-- Views must already filter the common cohort and dates.
-- Prefer export_raw_data.py when writing directly to the local raw folder.

COPY INTO @YOUR_APPROVED_STAGE/YOUR_UNIQUE_RUN/patients.csv
FROM (
SELECT
    PATIENT_ID AS patient_id,
    BIRTH_YEAR AS birth_year,
    GENDER AS gender,
    GEOGRAPHY AS geography,
    OBSERVATION_START AS observation_start,
    OBSERVATION_END AS observation_end
FROM {{PATIENTS_CANONICAL_VIEW}}
)
FILE_FORMAT = (TYPE = CSV COMPRESSION = NONE FIELD_OPTIONALLY_ENCLOSED_BY = '"' NULL_IF = ('') EMPTY_FIELD_AS_NULL = TRUE)
HEADER = TRUE SINGLE = TRUE OVERWRITE = FALSE MAX_FILE_SIZE = 268435456;

COPY INTO @YOUR_APPROVED_STAGE/YOUR_UNIQUE_RUN/medical_claims.csv
FROM (
SELECT
    CLAIM_ID AS claim_id,
    PATIENT_ID AS patient_id,
    CLAIM_DATE AS claim_date,
    AVAILABLE_DATE AS available_date,
    STATUS AS status,
    DIAGNOSIS_CODE AS diagnosis_code,
    PROCEDURE_CODE AS procedure_code,
    PROVIDER_ID AS provider_id,
    PLACE_OF_SERVICE AS place_of_service
FROM {{MEDICAL_CLAIMS_CANONICAL_VIEW}}
)
FILE_FORMAT = (TYPE = CSV COMPRESSION = NONE FIELD_OPTIONALLY_ENCLOSED_BY = '"' NULL_IF = ('') EMPTY_FIELD_AS_NULL = TRUE)
HEADER = TRUE SINGLE = TRUE OVERWRITE = FALSE MAX_FILE_SIZE = 268435456;

COPY INTO @YOUR_APPROVED_STAGE/YOUR_UNIQUE_RUN/pharmacy_claims.csv
FROM (
SELECT
    CLAIM_ID AS claim_id,
    PATIENT_ID AS patient_id,
    FILL_DATE AS fill_date,
    AVAILABLE_DATE AS available_date,
    STATUS AS status,
    DRUG_ID AS drug_id,
    THERAPY_CLASS AS therapy_class,
    QUANTITY AS quantity,
    DAYS_SUPPLY AS days_supply,
    PRESCRIBER_ID AS prescriber_id,
    PLAN_ID AS plan_id,
    PATIENT_COST AS patient_cost
FROM {{PHARMACY_CLAIMS_CANONICAL_VIEW}}
)
FILE_FORMAT = (TYPE = CSV COMPRESSION = NONE FIELD_OPTIONALLY_ENCLOSED_BY = '"' NULL_IF = ('') EMPTY_FIELD_AS_NULL = TRUE)
HEADER = TRUE SINGLE = TRUE OVERWRITE = FALSE MAX_FILE_SIZE = 268435456;

COPY INTO @YOUR_APPROVED_STAGE/YOUR_UNIQUE_RUN/providers.csv
FROM (
SELECT
    PROVIDER_ID AS provider_id,
    SPECIALTY AS specialty,
    GEOGRAPHY AS geography,
    ORGANIZATION AS organization
FROM {{PROVIDERS_CANONICAL_VIEW}}
)
FILE_FORMAT = (TYPE = CSV COMPRESSION = NONE FIELD_OPTIONALLY_ENCLOSED_BY = '"' NULL_IF = ('') EMPTY_FIELD_AS_NULL = TRUE)
HEADER = TRUE SINGLE = TRUE OVERWRITE = FALSE MAX_FILE_SIZE = 268435456;

COPY INTO @YOUR_APPROVED_STAGE/YOUR_UNIQUE_RUN/plans.csv
FROM (
SELECT
    PLAN_ID AS plan_id,
    PAYER_TYPE AS payer_type
FROM {{PLANS_CANONICAL_VIEW}}
)
FILE_FORMAT = (TYPE = CSV COMPRESSION = NONE FIELD_OPTIONALLY_ENCLOSED_BY = '"' NULL_IF = ('') EMPTY_FIELD_AS_NULL = TRUE)
HEADER = TRUE SINGLE = TRUE OVERWRITE = FALSE MAX_FILE_SIZE = 268435456;

COPY INTO @YOUR_APPROVED_STAGE/YOUR_UNIQUE_RUN/enrollment.csv
FROM (
SELECT
    PATIENT_ID AS patient_id,
    COVERAGE_START AS coverage_start,
    COVERAGE_END AS coverage_end,
    PLAN_ID AS plan_id
FROM {{ENROLLMENT_CANONICAL_VIEW}}
)
FILE_FORMAT = (TYPE = CSV COMPRESSION = NONE FIELD_OPTIONALLY_ENCLOSED_BY = '"' NULL_IF = ('') EMPTY_FIELD_AS_NULL = TRUE)
HEADER = TRUE SINGLE = TRUE OVERWRITE = FALSE MAX_FILE_SIZE = 268435456;

COPY INTO @YOUR_APPROVED_STAGE/YOUR_UNIQUE_RUN/therapy_mapping.csv
FROM (
SELECT
    DRUG_ID AS drug_id,
    THERAPY_CLASS AS therapy_class,
    EFFECTIVE_START AS effective_start,
    EFFECTIVE_END AS effective_end
FROM {{THERAPY_MAPPING_CANONICAL_VIEW}}
)
FILE_FORMAT = (TYPE = CSV COMPRESSION = NONE FIELD_OPTIONALLY_ENCLOSED_BY = '"' NULL_IF = ('') EMPTY_FIELD_AS_NULL = TRUE)
HEADER = TRUE SINGLE = TRUE OVERWRITE = FALSE MAX_FILE_SIZE = 268435456;

-- Download with GET in a client that supports local file transfer, e.g. SnowSQL.
-- Replace LOCAL_ABSOLUTE_RAW_PATH; run the pipeline against the downloaded folder.
-- GET is not a browser SQL-result download command.
GET @YOUR_APPROVED_STAGE/YOUR_UNIQUE_RUN/ file:///LOCAL_ABSOLUTE_RAW_PATH/;
