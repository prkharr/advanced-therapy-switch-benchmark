{
  "version": 1,
  "evidence_status": "reference",
  "description": "Original logistic reference; CPU-only alternative requiring core dependencies.",
  "selected": {
    "members": [
      {
        "name": "logistic_reference",
        "family": "logistic_regression",
        "options": {
          "C": 0.2,
          "max_iter": 1000,
          "class_weight": "balanced"
        },
        "reuse_legacy": true
      }
    ],
    "weights": [
      1
    ]
  }
}
